version: '3.8'

services:
  solveos:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: solveos-app
    ports:
      - "3000:3000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
    restart: always
